const stateText={idle:"Ready when you are.",listening:"Listening for a question.",thinking:"Looking through the work.",speaking:"Here is what I found."};
document.querySelectorAll("[data-state]").forEach(button=>button.addEventListener("click",()=>{const state=button.dataset.state;document.querySelectorAll("[data-state]").forEach(b=>b.classList.toggle("active",b===button));document.querySelectorAll("[data-state-output]").forEach(el=>el.textContent=stateText[state]||state)}));
document.querySelectorAll("[data-toggle]").forEach(button=>button.addEventListener("click",()=>{const target=document.querySelector(button.dataset.toggle);target?.classList.toggle("visible");button.classList.toggle("active")}));
document.querySelectorAll("[data-nori-state]").forEach(button=>button.addEventListener("click",()=>{const state=button.dataset.noriState;const character=document.querySelector(".nori-character");const output=document.querySelector("[data-nori-output]");character.className=`nori-character ${state}`;output.textContent=stateText[state]||"I can take you there."}));
document.querySelectorAll("[data-signal]").forEach(button=>button.addEventListener("click",()=>{const ring=document.querySelector(".signal-ring-large");const output=document.querySelector("[data-signal-output]");ring.classList.toggle("active");output.textContent=ring.classList.contains("active")?"Signal is active. Try a question below.":"Signal is resting."}));
